# MongoDB connection URI (local or Atlas)
MONGO_URI=mongodb://localhost:27017/lulimi_lingo

# Express server port
PORT=5000

# Optional: Python backend to proxy AI calls to (keep as-is while we migrate AI)
PYTHON_BACKEND_URL=http://localhost:8000

# Optional: API keys for direct AI provider usage
GEMINI_API_KEY=
OPENAI_API_KEY=
